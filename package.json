{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "father-build",
    "postinstall": "husky install"
  },
  "lint-staged": {
    "**/*.{js,json,css,md}": [
      "prettier -w",
      "git add"
    ],
    "**/*.{js}": [
      "npm run lint:fix",
      "git add"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "@types/node": "^15.12.1",
    "father-build": "^1.19.6",
    "husky": "^6.0.0",
    "jest": "^27.0.4",
    "lerna": "^4.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
